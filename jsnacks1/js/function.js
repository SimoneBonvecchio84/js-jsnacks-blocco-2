// genera array

/**
 * Description
 * @param{number} num 
 * @param{number}  
 * @returns {any}
 */
function generaNbr(min, max) {
    let numRandom = Math.floor(Math.random() * max - min + 1) + min;
    return numRandom
}